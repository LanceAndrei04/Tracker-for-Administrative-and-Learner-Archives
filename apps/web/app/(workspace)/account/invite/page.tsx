"use client";
import Link from "next/link";
import { useState } from "react";
import { Icon } from "@/components/ui/icon";
export default function InvitePage() { const [sent, setSent] = useState(false); return <><Link href="/account" className="back-link">← Account</Link><header className="page-heading"><div><h1>Invite teacher</h1><p>Create a teacher account with approved school access.</p></div></header><form className="record-form invite-form" onSubmit={(event) => { event.preventDefault(); setSent(true); }}><fieldset><legend>Teacher account</legend><p className="section-copy">Invitations are available only to Super Admins. The production action will provision an approved user and send a Supabase invitation.</p><div className="form-grid"><label>Full name<input required placeholder="Maria Cruz" /></label><label>School email<input type="email" required placeholder="maria.cruz@school.edu.ph" /></label><label>Role<select defaultValue="TEACHER"><option value="TEACHER">Teacher</option><option value="SUPER_ADMIN">Super Admin</option></select></label></div></fieldset><div className="form-actions"><Link href="/account" className="button button-secondary">Cancel</Link><button type="submit" className="button button-primary"><Icon name="user" />Send invite</button></div>{sent ? <p className="form-success" role="status">Invitation queued in the demo. Live delivery will use the guarded backend invite endpoint.</p> : null}</form></>; }
