import { Icon } from "@/components/ui/icon";
import { talaApi } from "@/lib/api/tala-api";

export default async function FilesPage() { const files = await talaApi.getFiles(); return <><header className="page-heading records-heading"><div><h1>Files</h1><p>Store and find school documents.</p></div><button className="button button-primary"><Icon name="upload" />Upload file</button></header><section className="records-controls"><label className="search-input"><Icon name="search" /><input placeholder="Search files..." /></label><div className="filter-set">{["Category", "School year", "Linked record"].map((label) => <button key={label} className="filter-button">{label}<Icon name="chevron" /></button>)}</div></section><section className="table-shell"><table className="record-table"><thead><tr><th>File</th><th>Linked to</th><th>Category</th><th>School year</th><th>Uploaded</th><th /></tr></thead><tbody>{files.map((file) => <tr key={file.id}><td className="file-cell"><span className="file-icon"><Icon name="files" /></span><span><b>{file.name}</b><small>{file.size}</small></span></td><td>{file.linkedTo}</td><td>{file.category}</td><td>{file.schoolYear}</td><td className="muted-cell">{file.uploaded}</td><td><button className="icon-button" aria-label={`More actions for ${file.name}`}><Icon name="more" /></button></td></tr>)}</tbody></table></section></>; }
